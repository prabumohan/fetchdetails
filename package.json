{
  "name": "fetchdetails",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev",
    "build": "node build.js",
    "build:cf": "npm run build",
    "deploy": "echo 'Deployment handled automatically by Cloudflare Pages'"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
