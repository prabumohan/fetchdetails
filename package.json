{
  "name": "fetchdetails",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev",
    "build": "echo 'No build needed for static site'",
    "build:cf": "echo 'No build needed for static site'",
    "deploy": "echo 'Deployment handled automatically by Cloudflare Pages'"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
